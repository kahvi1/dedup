{
  "name": "dedup",
  "version": "0.1.0",
  "description": "dedup - drag and drop data deduplication desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}